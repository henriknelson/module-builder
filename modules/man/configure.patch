--- old/configure	2019-03-10 09:56:43.000000000 +0000
+++ ./configure	2019-08-11 10:12:31.059295725 +0000
@@ -34,17 +34,13 @@
 # such that nothing can leak in from the environment.
 
 SOURCEDIR=`dirname "$0"`
-
-MANPATH_BASE="/usr/share/man:/usr/X11R6/man"
-MANPATH_DEFAULT="/usr/share/man:/usr/X11R6/man:/usr/local/man"
+MANPATH_BASE="@MAGISK_PREFIX@/usr/share/man"
+MANPATH_DEFAULT="@MAGISK_PREFIX@/usr/share/man"
 OSENUM=
 OSNAME=
 UTF8_LOCALE=
 
-CC=`printf "all:\\n\\t@echo \\\$(CC)\\n" | env -i make -sf -`
-CFLAGS=
 LDADD=
-LDFLAGS=
 LD_NANOSLEEP=
 LD_OHASH=
 LD_RECVMSG=
@@ -126,7 +122,7 @@
 INSTALL_LIB=
 INSTALL_MAN=
 INSTALL_DATA=
-LN="ln -f"
+LN="ln -s -f"
 
 # --- manual settings from configure.local -----------------------------
 
@@ -367,7 +363,7 @@
 elif singletest nanosleep NANOSLEEP; then
 	:
 elif runtest nanosleep NANOSLEEP "-lrt"; then
-	LD_NANOSLEEP="-lrt"
+	LD_NANOSLEEP=""
 fi
 if [ "${HAVE_NANOSLEEP}" -eq 0 ]; then
 	echo "FATAL: nanosleep: no" 1>&2
@@ -452,7 +448,7 @@
 [ ${HAVE_GETLINE} -eq 0 ] && echo "#include <stdio.h>"
 
 echo
-echo "#define MAN_CONF_FILE \"/etc/${MANM_MANCONF}\""
+echo "#define MAN_CONF_FILE \"@MAGISK_PREFIX@/etc/${MANM_MANCONF}\""
 echo "#define MANPATH_BASE \"${MANPATH_BASE}\""
 echo "#define MANPATH_DEFAULT \"${MANPATH_DEFAULT}\""
 echo "#define OSENUM ${OSENUM}"
