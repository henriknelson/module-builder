--- fd-8.2.1/Cargo.toml	2020-12-07 17:57:45.000000000 +0000
+++ ./Cargo.toml	2021-01-16 19:36:11.647728483 +0000
@@ -61,8 +61,8 @@
 # FIXME: Re-enable jemalloc on macOS
 # jemalloc is currently disabled on macOS due to a bug in jemalloc in combination with macOS
 # Catalina. See https://github.com/sharkdp/fd/issues/498 for details.
-[target.'cfg(all(not(windows), not(target_os = "android"), not(target_os = "macos"), not(target_env = "musl")))'.dependencies]
-jemallocator = "0.3.0"
+#[target.'cfg(all(not(windows), not(target_os = "android"), not(target_os = "macos"), not(target_env = "musl")))'.dependencies]
+#jemallocator = "0.3.0"
 
 [dev-dependencies]
 diff = "0.1"
